# Base URL of the backend API. Public by design — it points at our own
# backend, not a secret. Never put ANTHROPIC_API_KEY, OPENAI_API_KEY, or any
# S3/storage credentials in this file or anywhere under app/ or components/.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
