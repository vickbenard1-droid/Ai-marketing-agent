# syntax=docker/dockerfile:1
# Development image: `next dev`, source bind-mounted by docker-compose.yml.
FROM node:20-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

EXPOSE 3000
CMD ["npm", "run", "dev"]
